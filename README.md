# libft-42-Project-
Libft is a custom implementation of a subset of the C standard library, including string manipulation, memory management, character checks, conversions, and file descriptor output functions.
